---
name: trash
description: "[Trash] Off-topic chit-chat unrelated to any project/habit goes here. Routed here during memory consolidation; never enters the memory tree, never used as a basis for reasoning. Can be cleared out periodically."
metadata:
  type: reference
  weight: 1
---

# Trash

During memory consolidation, **chit-chat / off-topic content unrelated to any project or any habit** → dump it here.

- Never enters the memory tree, never used as a basis for reasoning (it's the equivalent of "trash from before the marker," just explicitly collected).
- Can be cleared out periodically.
- Not sure whether it's trash (might be project-related but unverified)? → Don't dump it here; dump it in [`pending-review.md`](pending-review.md).

(empty)
