---
name: habit-action-hooks
description: "【Habit memory · lineage + appendix】The 'rule body' of the action-hook chain is now inlined and always-resident at the top of MEMORY.md, in the '🪝 Habit Memory · Action Hooks' table (user, 2026-07-01: habits = thinking habits, they should be always-resident by nature). This file is downgraded: it keeps only the 🩸 lineage index for changing this chain + the rationale; to add/iterate a hook, edit that table in MEMORY.md, not here"
metadata: 
  node_type: memory
  type: feedback
  category: habit
  weight: 4
  originSessionId: ""
---

⚠️**The rule body has moved out** → the rule body of the action-hook chain (read the requirement → before thinking → replying to the requirement → before executing → during execution → after executing → before pushing → generating docs → after pushing → memory consolidation) **now lives always-resident at the top of `MEMORY.md`, in the '🪝 Habit Memory · Action Hooks' table**.

**Why it moved** (user, 2026-07-01): **Habit memory = my thinking habits; I have to carry them every time I think, they should be in my head every single day by nature** (verbatim: "habit memory should be always-resident by nature, those are your thinking habits"). Before, it sat in this file and MEMORY.md only hung a one-line pointer, relying on "read it first" — and I would "forget to flip to it" and miss it. That's exactly how the date-bracket rule silently stopped working. Habits are a **bounded small set**, and each one must always fire, so the whole chain rightly belongs inlined into that index which auto-loads every window.

**This file is downgraded = lineage + rationale appendix** (pull on demand, not always-resident):
- **Editing the hook chain = editing that table in MEMORY.md** (not here); this file no longer holds the rule body, to avoid two copies drifting and violating "one fact, one place."
- Habit = **action-triggered** (not topic-triggered), ordered by workflow stage, fires on cue; when consolidating, hold to [[no-contradiction-when-writing-memory]].
