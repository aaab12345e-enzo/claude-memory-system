# 踩坑档案

每个实质任务一份 `<日期>-<任务>.md`。碰到**类似主题**的新需求时,据此提前避开以前踩过的坑。

每个坑记 5 行(第 4 行最值钱):

> 我以为/做了 · 怎么暴露的 · 真因 · **★为啥会上当(误导信号——下次靠它提前识别)** · 改法

约定:① 标日期;② 它是**历史现场,不改写**(改了就失真);③ 复发或重要的坑 → 升华成一条(常高权重)避错记忆,权重 +1。

> 空目录,按需往里加。
